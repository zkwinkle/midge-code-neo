[project]
name = "midge-badge-framework"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "josfemova", email = "josfemova@gmail.com" }
]
requires-python = ">=3.10"
dependencies = [
    "bleak>=2.1.1",
]

#[project.scripts]
#midge-badge-framework = "midge_badge_framework:main"

[build-system]
requires = ["uv_build>=0.10.8,<0.11.0"]
build-backend = "uv_build"
